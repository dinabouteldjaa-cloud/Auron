import type { CapacitorConfig } from '@capacitor/cli'

// NOTE: appId is a placeholder. If you already registered a bundle ID with
// Apple/Firebase, change this to match it exactly before building.
const config: CapacitorConfig = {
  appId: 'com.auron.app',
  appName: 'Auron',
  webDir: 'dist',
}

export default config
